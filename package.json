{
  "name": "hobbyist",
  "version": "0.1.0-alpha.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --build --force",
    "test": "tsc --build && node --test packages/*/dist/test/*.test.js packages/studio/dist-test/test/*.test.js",
    "build": "tsc --build && npm run build -w @hobby.sh/studio"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3"
  }
}
