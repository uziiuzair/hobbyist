.claude
.remember
.superpowers
node_modules
dist
dist-test
.opencode

# Generated by `bun install` during every build. package-lock.json is the
# committed lockfile; this one is bun's own and would otherwise leave every
# built checkout permanently dirty, which blocked `hobby update`.
bun.lock
